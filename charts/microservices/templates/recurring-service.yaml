apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.recurring.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.recurring.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: recurring-service
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.recurring.service.port }}
    targetPort: {{ .Values.recurring.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.recurring.name }}
