---
id: 002
title: Dapr Infrastructure Abstraction Implementation Plan
stage: plan
date: 2026-02-11
surface: agent
model: claude-sonnet-4-5-20250929
feature: 010-dapr-abstraction
branch: 010-dapr-abstraction
user: user
command: /sp.plan
labels: ["plan", "dapr", "event-driven", "infrastructure-abstraction", "recurring-tasks", "reminders", "phase-v"]
links:
  spec: specs/010-dapr-abstraction/spec.md
  ticket: null
  adr: null
  pr: null
files:
  - specs/010-dapr-abstraction/plan.md
  - specs/010-dapr-abstraction/research.md
  - specs/010-dapr-abstraction/data-model.md
  - specs/010-dapr-abstraction/contracts/tasks-api.yaml
  - specs/010-dapr-abstraction/contracts/reminders-api.yaml
  - specs/010-dapr-abstraction/contracts/search-api.yaml
  - specs/010-dapr-abstraction/quickstart.md
tests:
  - none
---

## Prompt

```text
Spec 10: Advanced Logic & Dapr Abstraction

Create:
- **Dapr Architecture Sketch:** Define the sidecar relationship between the FastAPI app and Dapr components (State, Pub/Sub, Jobs).
- **Service Structure:** Refactor the Backend into a Dapr-ready directory structure (e.g., `/app`, `/components`, `/dapr_sdk_utils`).
- **Research Approach:** Use `context7` to pull the specific Dapr Python SDK syntax for "Service Invocation" and "Jobs API" to avoid legacy patterns.
- **Quality Validation:** Verify that no raw environment variables or hardcoded DB strings are used in logic (must be via Dapr Secrets/State).

Decisions needing documentation:
- **State Store Backend:** Tradeoffs between using Dapr's `state.postgresql` component vs. `state.redis` for conversation history.
- **Reminders Logic:** Choosing between the Dapr **Jobs API** (exact time) vs. **Cron Bindings** (polling). *Recommended: Jobs API.*
- **Communication Protocol:** Choosing between HTTP and gRPC for Dapr sidecar communication.

Testing strategy:
- **Logic Validation:** Unit tests for Recurring Task date calculations (Daily/Weekly/Monthly).
- **Infrastructure Mocking:** Verify the FastAPI app can "publish" to the Dapr endpoint even if Kafka is not yet physically present (using Dapr's mock pubsub).
- **Connectivity Check:** Confirm the Next.js 16+ frontend can successfully "invoke" a backend method through the Dapr sidecar port (3500).

Technical details:
- **Context7 Integration:** Continuously research while coding to ensure compatibility with Next.js 16.1.2 and Dapr 1.14+.
- **Zero-Manual-Code Policy:** All Dapr Component YAMLs and Python refactors must be generated by AI agents.
- **Phases:** - Phase 1: Research & Dapr Initialization (Sidecar setup).
  - Phase 2: Foundation (State Store & Secrets migration).
  - Phase 3: Advanced Features (Recurring Tasks & Jobs API).
  - Phase 4: Synthesis (Integration with Next.js 16 frontend).
```

## Response snapshot

Successfully created comprehensive implementation plan for Spec 010: Advanced Task Management with Infrastructure Abstraction.

**Planning Artifacts Created**:

1. **plan.md** (340 lines): Complete implementation plan with:
   - Technical context (Python 3.13+, FastAPI, Dapr v1.14+, Next.js 16.1.2)
   - Constitution check (all Phase-V requirements satisfied)
   - Project structure (backend with Dapr components, frontend with Service Invocation)
   - Event-Driven Architecture decisions (sync vs async patterns, Dapr components, event schema, tracing strategy)
   - Cloud provider neutrality strategy (zero code changes for infrastructure swaps)

2. **research.md** (10 sections): Dapr SDK integration patterns:
   - Dapr Python SDK with FastAPI (dapr-ext-fastapi)
   - State API for PostgreSQL access (eliminates direct DB dependencies)
   - Pub/Sub API for event streaming (Kafka abstraction)
   - Secrets API for configuration (no environment variables)
   - Jobs API for reminder scheduling (no polling)
   - Next.js 16 Service Invocation patterns
   - Event subscription with idempotency (correlation IDs)
   - Component configuration (YAML-based)
   - Testing strategies (mock components + integration tests)

3. **data-model.md** (10 sections): Entity definitions and relationships:
   - Task entity (with recurrence, priority, due_date, tags)
   - Recurrence Pattern entity (Daily/Weekly/Monthly calculation logic)
   - Reminder entity (scheduled notifications via Dapr Jobs)
   - Priority Level entity (High/Medium/Low)
   - Tag entity (user-defined labels)
   - Event schema (standardized format for Pub/Sub)
   - Dapr State Store key patterns
   - Data integrity rules and validation
   - Performance considerations (indexing, caching, query optimization)
   - Migration strategy (PostgreSQL → Dapr State Store)

4. **API Contracts** (3 OpenAPI specs):
   - tasks-api.yaml: Task CRUD, recurring tasks, priority management, completion endpoint
   - reminders-api.yaml: Reminder scheduling, cancellation, Dapr Jobs callback endpoint
   - search-api.yaml: Keyword search, filtering (status/priority/tags/due_date), sorting, natural language queries

5. **quickstart.md** (10 sections): Local development setup:
   - Dapr initialization in Kubernetes (Minikube)
   - Backend setup with Dapr sidecar
   - Dapr component configuration (State Store, Pub/Sub, Secrets)
   - Frontend setup with Service Invocation
   - End-to-end testing procedures
   - Monitoring and debugging (Dapr logs, dashboard, Zipkin tracing)
   - Common issues and solutions
   - Development workflow and useful commands

**Key Architectural Decisions**:

1. **Dapr State API for PostgreSQL**: Eliminates direct SQLAlchemy/psycopg2 dependencies, enables cloud-agnostic data access
2. **Dapr Pub/Sub for Events**: Decouples services, supports multiple providers (Kafka, Redpanda, RabbitMQ) with zero code changes
3. **Dapr Jobs API for Reminders**: Exact-time scheduling without polling (constitutional requirement)
4. **HTTP-based Service Invocation**: Next.js frontend → Dapr sidecar (port 3500) → FastAPI backend
5. **Correlation IDs for Idempotency**: Prevents duplicate recurring task generation on event replay
6. **YAML-based Component Configuration**: Infrastructure-as-Code, swappable providers

**Constitution Compliance**: ✅ ALL CHECKS PASSED
- Phase-V Event-Driven Architecture requirements satisfied
- Dapr sidecar pattern for all services
- Infrastructure abstraction (no direct dependencies)
- Event schema standardization
- Dapr Jobs API (no polling)
- Dapr Secrets API (no environment variables)
- Distributed tracing with correlation IDs
- Cloud provider neutrality

**Agent Context Updated**: Claude Code context file updated with new technologies (Dapr Python SDK, Next.js 16, OpenAI Agents SDK)

## Outcome

- ✅ Impact: Complete Phase-V implementation plan with Dapr infrastructure abstraction, event-driven architecture, and comprehensive API contracts
- 🧪 Tests: Testing strategies documented (unit tests for recurrence logic, Dapr mock components, integration tests for event flows)
- 📁 Files: Created plan.md, research.md, data-model.md, 3 API contracts (tasks, reminders, search), quickstart.md
- 🔁 Next prompts: Run `/sp.tasks` to generate implementation tasks; Consider documenting architectural decisions with `/sp.adr`
- 🧠 Reflection: Successfully designed Phase-V architecture with complete Dapr abstraction. All infrastructure dependencies eliminated from application code. Event-driven patterns enable loose coupling and horizontal scaling. Recurring task generation via event subscription ensures idempotency. Dapr Jobs API provides precise reminder scheduling without polling overhead.

## Evaluation notes (flywheel)

- Failure modes observed: None - planning workflow completed successfully
- Graders run and results (PASS/FAIL): PASS - All Phase-V constitutional requirements satisfied, comprehensive research and design artifacts created
- Prompt variant (if applicable): Standard /sp.plan workflow with Phase-V event-driven architecture focus
- Next experiment (smallest change to try): Proceed to `/sp.tasks` to generate implementation tasks; Document significant architectural decisions (Dapr adoption, event-driven patterns, Jobs API) with `/sp.adr`
